#include "monty.h"

/**
  * t_stack - prints the top of the stack
  * @head: stack head
  * @numb: line_number
  *
  * Return: no return
  */
void t_stack(stack_t **head, unsigned int numb)
{
        (void)head;
        (void)numb;
        bus.lifi = 0;
}
